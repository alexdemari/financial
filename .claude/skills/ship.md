---
name: ship
description: Run tests, verify, commit, push.
---
1. Run `just test` — report N/N passing
2. Run `just fmt` — re-stage se necessário
3. Mostrar `git diff --stat` e propor commit message convencional
4. Commit + push após confirmação
